{
  "name": "EscrowDecentraland",
  "version": "0.0.1",
  "description": "multi-player decentraland escrow app",
  "main": "index.js",
  "scripts": {
    "build": "yarn",
    "start": "nodemon --watch backend --exec node backend/server.js"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "socket.io": "^2.2.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "engines": {
    "node": "17.2.0",
    "npm": "6.9.0"
  }
}
